# Design iterations

Preserve existing HTML and its styling when trying design changes. Copy the current version to a new numbered HTML file with a separate stylesheet before editing, so the user can compare versions side by side. Do not replace index.html with a design iteration unless explicitly requested. Update compare.html to show the relevant versions.
